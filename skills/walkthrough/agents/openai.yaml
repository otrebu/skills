interface:
  display_name: "Walkthrough"
  short_description: "Review a list of items one at a time"
  default_prompt: "Use $walkthrough to step through these items one at a time."
policy:
  allow_implicit_invocation: true
