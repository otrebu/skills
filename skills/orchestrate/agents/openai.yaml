interface:
  display_name: "Orchestrate"
  short_description: "Supervise detached agent-CLI workers"
  default_prompt: "Use $orchestrate to delegate this task to supervised detached workers and integrate the verified result."
policy:
  allow_implicit_invocation: false
