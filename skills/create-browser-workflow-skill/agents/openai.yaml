interface:
  display_name: "Create Browser Workflow Skill"
  short_description: "Turn a web-app workflow into an agent-browser skill"
  default_prompt: "Use $create-browser-workflow-skill to build a skill that operates this web application through agent-browser."
policy:
  allow_implicit_invocation: true
