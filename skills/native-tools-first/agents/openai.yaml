interface:
  display_name: "Native Tools First"
  short_description: "Right-size builds around existing capabilities"
  default_prompt: "Use $native-tools-first to scope and implement this requirement around existing tools before adding custom machinery."
policy:
  allow_implicit_invocation: true
