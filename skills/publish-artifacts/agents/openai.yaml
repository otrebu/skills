interface:
  display_name: "Publish Artifacts"
  short_description: "Publish an HTML artifact tree to ubuilt.dev"
  default_prompt: "Use $publish-artifacts to publish this folder to ubuilt.dev."
policy:
  allow_implicit_invocation: false
